# Phase 01 — Next.js Scaffold

## Goal

Create a minimal Next.js 16 app with TypeScript, Tailwind CSS 4, and ESLint. No database code yet.

## Files added

- `package.json`, `package-lock.json`
- `next.config.ts`, `tsconfig.json`, `eslint.config.mjs`, `postcss.config.mjs`
- `src/app/layout.tsx`, `src/app/globals.css`, `src/app/favicon.ico`
- `public/` static assets
- `.gitignore`

## Verification

```bash
npm install
npm run dev
```

Open http://localhost:3000 — default Next.js page should load.

## Git

Branch: `feature/01-scaffold`  
Commit: `Scaffold Next.js app with TypeScript and Tailwind`
